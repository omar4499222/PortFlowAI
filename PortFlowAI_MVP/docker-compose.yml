services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
  dashboard:
    build: .
    command: streamlit run dashboard/app.py --server.address=0.0.0.0
    ports:
      - "8501:8501"
    depends_on:
      - api
    volumes:
      - .:/app
